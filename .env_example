# Configuração de Variáveis de Ambiente
# 
# Copie este arquivo para .env e ajuste os valores conforme necessário:
#   cp .env_example .env
#
# IMPORTANTE: O arquivo .env não deve ser commitado no Git (já está no .gitignore)

# Nome do serviço para OpenTelemetry
# Este nome aparecerá nos traces, métricas e logs
OTEL_SERVICE_NAME=poc-fastapi-service

# Endpoint do OpenTelemetry Collector
# Formato: http://host:port (HTTP) ou https://host:port (HTTPS)
# O código automaticamente converte para gRPC (porta 4317) quando necessário
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318

# Nível de log da aplicação
# Valores possíveis: DEBUG, INFO, WARNING, ERROR, CRITICAL
# DEBUG: mostra todos os logs (incluindo debug)
# INFO: mostra logs informativos e acima
# WARNING: mostra apenas warnings e erros
# ERROR: mostra apenas erros
# CRITICAL: mostra apenas erros críticos
LOG_LEVEL=INFO

